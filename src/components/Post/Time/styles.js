import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  timeWrapper: {
    padding: 10,
  },
  timeText: {
    color: 'gray',
    fontSize: 13,
  },
  translateText: {
    fontWeight: 'bold',
    fontSize: 13,
  },
});

export default styles;

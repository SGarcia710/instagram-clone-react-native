import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  mediaViewerWrapper: {
    height: 350,
  },
  backgroundVideo: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
  },
  paginationDots: {
    position: null,
    bottom: -25,
  },
});

export default styles;
